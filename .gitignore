# Python virtual environment
/venv/

# Python cache files
__pycache__/
*.pyc

# Log files
logs/

# Database file (contains user memory and private data)
safi_memory.db

# Any local environment files (if you create them later)
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
dist/
build/
.pytest_cache/
.coverage
htmlcov/
debug.py
teams_bot.py
telegram_bot.py
whatsapp_bot.py
cache/

# Node / frontend
node_modules/
.next/
out/
coverage/
.cache/

# OS/editor
.DS_Store
Thumbs.db
.idea/
.vscode/
vector_store/
tts_cache/
tests/logs/
tests/__pycache__/

# Test files
test.py

# Benchmarks
/benchmarks

# Chat sub-project dependencies
chat/node_modules/
chat/package.json
chat/package-lock.json
chat/tailwind.config.js
chat/tailwindcss/

# Claude Code — local config and instructions (not for version control)
.claude/
CLAUDE.md

# Local utility / dev scripts (not for version control)
driver.py
load.py
purge_profile.py

# Database backups / dumps
*.sql

# Local backups (e.g. policy values/weights snapshots)
.backups/

# Benchmark backup/scratch snapshots (top-level only; tracked results live in Benchmarks/Results & Scripts)
/Benchmarks/*.json

# Local drafts / blog posts (not for version control)
docs/building-and-managing-agents-post.md

# Internal-only docs (gap analyses, security posture — never publish)
docs/internal/
